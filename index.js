var katzDeliLine = [];

function takeANumber(katzDeliLine, name) {
  katzDeliLine.push(katzDeliLine, name); 
  var position = katzDeli.length; 
  return "Welcome, " + name + ". You are number " position + "in line."
}

